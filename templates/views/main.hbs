<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hall Ticket Form-KR Techno</title>
    <link rel="shortcut icon" type="x-icon" href="./css/logo.png">  
    
    <style>
        * {
        margin: 0;
        padding: 0;
        box-sizing: border-box; /* Include padding and border in element's total width and height */
    }
    body {
        font-family: Arial, sans-serif;
        background: -webkit-linear-gradient(left, #003366, #004080, #0059b3, #0073e6);
        overflow-x: hidden;
        margin: 0;
    }
    .container {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        width: 80%;
        padding: 20px;
        margin: 0 auto;
    }
    .form-container {
        width: 100%;
        max-width: 600px;
        padding: 20px;
        background: #fff;
        border-radius: 5px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        border: none;
        margin: 20px auto; /* Add margin for better spacing */
    }
    .form-group {
        display: flex;
        flex-direction: column;
        margin-bottom: 10px;
    }
    label {
        margin-bottom: 5px;
        margin-right: 10px;
    }
    input[type="text"],
    input[type="email"],
    input[type="tel"],
    select {
        padding: 10px;
        border-radius: 5px;
        border: 1px solid #ccc;
        background-color: #f5d04c;
        width: 100%;
    }
    .button-container {
        display: flex;
        justify-content: space-between;
        margin-top: 20px;
    }
    button {
        padding: 10px 20px;
        border-radius: 5px;
        cursor: pointer;
        border: none;
    }
    button[type="submit"] {
        background-color: #007bff;
        color: white;
    }
    button[type="button"] {
        background-color: #0e86ef;
        color: white;
    }
    button:hover {
        opacity: 0.8;
    }
    input[type="text"]:valid,
    input[type="email"]:valid,
    input[type="tel"]:valid,
    select:valid {
        background-color: rgb(223, 243, 250);
    }
    footer {
        position: relative;
        left: 0;
        bottom: 0;
        width: 100%;
        color: #fff;
        text-align: center;
        padding: 10px;
    }
    header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 10px 20px;
        color: #fff;
        top: 0;
        width: 100%;
        position: sticky;
    }
    header h1 {
        font-size: 40px;
        color: #fff;
        margin: 0;
    }
    .nav-container {
        display: flex;
        justify-content: flex-end; /* Align menu to the right */
        padding: 10px 20px;
        color: #fff;
    }
    nav ul {
        margin: 0;
        padding: 0;
        list-style: none;
        display: flex;
        align-items: center;
    }
    nav ul li {
        margin-right: 10px;
    }
    nav ul li button {
        text-decoration: none;
        color: #fff;
        font-weight: bold;
        transition: color 0.3s ease;
        cursor: pointer;
        background: none;
        border: none;
    }
    nav ul li button:hover {
        color: #fca37b;
    }
    .menu-btn {
        display: none;
        font-size: 24px;
        cursor: pointer;
        color: #fff;
        background: none;
        border: none;
        margin-right: 20px;
    }
    @media (max-width: 768px) {
        header {
            
            background: rgba(0, 0, 0, 0.8);

        }
        .menu-btn {
            display: block;
        }
        nav ul {
            display: none;
            flex-direction: column;
            width: 100%;
            background: rgba(0, 0, 0, 0.8);
            position: absolute;
            top: 100%; /* Position dropdown below header */
            right: 0; /* Align to the right */
        }
        .menu-btn.active + .nav-container ul {
            display: flex;
        }
         
        footer {
            padding: 5px;
            position: relative;
            left: 0;
            bottom: 0;
            width: 100%;
            color: #fff;
            text-align: center;
            padding: 10px;
        }
    }
    @media (max-width: 480px) {
        .form-container {
            width: 90%;
            padding: 20px;
        }
        header h1 {
            font-size: 18px;
        }
        input[type="text"],
        input[type="email"],
        input[type="tel"],
        select {
            padding: 8px;
            font-size: 14px;
        }
        button[type="submit"],
        button[type="button"] {
            font-size: 14px;
            padding: 8px 16px;
        }
        }
    </style>
   
</head>
<body onload="onpageload()">
    
<div class="contaner">

    <header>
        <h1 class="company-name">KR TECHNOLOGIES PVT LTD</h1>
          <button class="menu-btn">&#9776;</button>
    <nav class="nav-container">
    <ul>
      <li><a href="/">
      <button type="button">Home</button></li>
      </a>
      <li><a href="/about">
      <button type="button">About</button>
      </a>
      </li>
      <li><a href="/result">
      <button type="button">Results</button></li>
      </a>
         <li><a href="/logout">
      <button type="button">Logout</button></li>
      </a>
    
    </ul>
    </nav>    
    </header>
    
    <div class="form-container">
        <form id="hallTicketForm" action="/generateTicket" method="post">
            <div class="form-group">
                <label for="name">Name:</label>
                <input type="text" id="name" name="name" required>
            </div>
            <div class="form-group">
                <label for="fatherName">Father's Name:</label>
                <input type="text" id="fatherName" name="fatherName" required>
            </div>
            <div class="form-group">
                <label for="mobile">Mobile:</label>
                <input type="tel" id="mobile" name="mobile" required>
            </div>
            <div class="form-group">
                <label for="email">Email:</label>
                <input type="email" id="email" name="email" required>
            </div>
            <div class="form-group">
                <label for="preference1">First Preference:</label>
                <select name="preference1" id="preference1" required onchange="updatePreferences()">
                    <option value="">Select...</option>
                    <option value="Hyderabad">Hyderabad</option>
                    <option value="Secundrabad">Secundrabad</option>
                    <option value="Uppal">Uppal</option>
                    <option value="Gachibowli">Gachibowli</option>
                    <option value="Warangal">Warangal</option>
                </select>
            </div>
            <div class="form-group">
                <label for="preference2">Second Preference:</label>
                <select name="preference2" id="preference2" required>
                    <option value="">Select...</option>
                    <option value="Hyderabad">Hyderabad</option>
                    <option value="Secundrabad">Secundrabad</option>
                    <option value="Uppal">Uppal</option>
                    <option value="Gachibowli">Gachibowli</option>
                    <option value="Warangal">Warangal</option>
                </select>
            </div>
            <div class="form-group">
                <label for="preference3">Third Preference:</label>
                <select name="preference3" id="preference3" required>
                    <option value="">Select...</option>
                    <option value="Hyderabad">Hyderabad</option>
                    <option value="Secundrabad">Secundrabad</option>
                    <option value="Uppal">Uppal</option>
                    <option value="Gachibowli">Gachibowli</option>
                    <option value="Warangal">Warangal</option>
                </select>
            </div>
            <div class="button-container">
                <button type="submit" id="generateButton">Generate Hall Ticket</button>
                <button type="button">Clear</button>
            </div>
        </form>
    </div>
    
    {{>footer}}
</div>
<script>
     // JavaScript for menu button toggle
    document.addEventListener('DOMContentLoaded', function() {
        const menuBtn = document.querySelector('.menu-btn');
        const nav = document.querySelector('.nav-container ul');

        menuBtn.addEventListener('click', function(e) {
            this.classList.toggle('active');
            nav.classList.toggle('active');
            e.stopPropagation();
        });

        // Close menu when clicking outside
        document.addEventListener('click', function(e) {
            if (!menuBtn.contains(e.target) && !nav.contains(e.target)) {
                menuBtn.classList.remove('active');
                nav.classList.remove('active');
            }
        });
    });
</script>
    <script src="./js/main.js"></script>
</body>
</html>